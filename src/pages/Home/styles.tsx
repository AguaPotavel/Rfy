import styled from 'styled-components'

import { LeftSidebar } from '../Application/styles'

export const Container = styled.div`
    display: flex;
    width: calc(100% - 15.6rem);
    position: absolute;
    top:0;
    left: 15.6rem;
    background: red;
`;